# Bloret-Launcher-Setup
百络谷启动器在线安装程序

基于 PyQt6 和 QFluentWidgets 的安装向导程序。

## 功能特点

- 🎨 现代化的 Fluent Design 界面
- 📦 支持快速安装和自定义安装
- 📁 可自定义安装路径
- 🖥️ 可选桌面快捷方式和启动菜单项
- 📊 实时安装进度显示
- 🌐 中英文双语界面

## 安装依赖

```bash
pip install -r requirements.txt
```

## 运行程序

```bash
python installer.py
```

## 界面预览

### 1. 欢迎页面
- 显示应用 Logo 和基本信息
- 提供快速安装和自定义安装选项

### 2. 安装路径选择
- 默认安装到 AppData 目录
- 支持自定义安装路径

### 3. 附加选项
- 可选桌面快捷方式
- 可选启动菜单项

### 4. 安装进度
- 实时显示安装进度
- 安装完成后自动关闭

## 项目结构

```
Bloret-Launcher-Setup/
├── installer.py          # 主程序
├── page1.py              # 欢迎页面
├── page2_1.py            # 安装路径选择页面
├── page2_2.py            # 附加选项页面
├── page3.py              # 安装进度页面
├── requirements.txt      # 依赖文件
├── README.md             # 说明文档
└── ui/                   # UI 资源文件
    ├── bloret.png        # 应用图标
    ├── bloret.ico        # 窗口图标
    ├── BLroundHome.png   # 背景图片
    └── *.ui              # Qt Designer 文件
```

## 自定义说明

- 替换 `ui/bloret.png` 和 `ui/bloret.ico` 可以更改应用图标
- 替换 `ui/BLroundHome.png` 可以更改安装页面的背景图片
- 修改各页面的文本内容可以自定义显示信息

## 注意事项

- 确保已安装 PyQt6 和 QFluentWidgets
- 程序需要管理员权限来创建快捷方式和写入系统目录
- 安装路径需要确保有足够的磁盘空间

> [!WARNING]
> 本仓库因仓库所有者 @Detritalw 而参与 [百日会战计划 #20](https://github.com/BloretCrew/Bloret-Launcher/discussions/20) ，暂时停止维护，约 100 天后解除。  
> 如有问题，请找 @RhedarLiu